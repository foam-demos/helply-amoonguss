# Knowledge Bridge

Python service that analyzes ticket patterns using embeddings, identifies documentation gaps, and auto-generates knowledge base article suggestions.

**Stack:** Python 3.11, FastAPI, Celery, OpenAI embeddings, Pinecone

**Local setup:** `pip install -r requirements.txt && uvicorn main:app --reload`