# Realtime Gateway

Node.js/TypeScript microservice managing WebSocket connections for live agent handoffs and real-time chat routing via Matrix protocol.

**Stack:** Node.js 20, TypeScript 5.3, Express, ws, matrix-js-sdk

**Local setup:** `npm install && npm run build && npm run dev`